# 本轮质检结论

时间：2026-04-03

## 已通过项

1. `采区智能规划设计一体化方法与系统.docx` 已重新生成，公式对象链路启用，写入公式 39 个。
2. `docx_research_qa.py` 检查通过：
   - 公式对象数量满足要求；
   - 中文公式编号连续；
   - DOCX 公式质检通过。
3. `stats_reporting_check.py` 严格模式通过：
   - `metrics`、`uncertainty`、`significance`、`effect_size`、`sample_size`、`split_leakage` 六类关键词均已覆盖。
4. `reference_integrity_check.py` 严格模式通过：
   - 参考文献总数 25；
   - 已验证 25；
   - 未解析 0。
5. 主稿图片引用检查通过：
   - Markdown 图片引用 8 处；
   - 缺失图片 0 处；
   - DOCX 内嵌图片 8 张。

## 主要改进

1. 摘要、引言末段、结果、讨论和结论已按正式论文语气重写，弱化“系统说明书”口吻。
2. 正文已显式区分“中等结论”和“探索性结论”，避免样例级证据被写成强外推结论。
3. 已把样例方案中的工程边界写入正文：
   - 3 个工作面均生成成功；
   - 但均存在“推进长度小于最小值 800.0 m”的校验提示；
   - 因而当前结果应理解为候选设计对象和链路贯通证据，而非工程终判方案。
4. 已新增“证据映射与复现约束”小节，明确样例数据、接口结果、脚本图件与代码快照 `b0d24be` 的对应关系。

## 未完全通过项

1. 对完整主稿运行 `claim_strength_audit.py --strict --require-labels` 时，脚本仍会把表格块和参考文献标题识别为候选主张。
2. 对去掉参考文献后的正文单独运行同一检查时，仅剩 1 处未显式标注候选主张。
3. 经人工核查，这 1 处为 `表2` 的 Markdown 表格块整体被误判，并非正文核心论断。
4. 正文核心结论已补足显式标签，且 `risky_strong_claims=0`。

## 当前硬伤

1. 作者、单位、基金、首页脚注等投稿元信息仍未补齐。
2. 论文仍缺真实矿井案例、基线对照和参数标定结果，因此现阶段更接近“高质量方法稿/重构稿”，尚不是“直接送审稿”。
3. 样例布局虽已贯通，但 3 个工作面仍存在“推进长度小于最小值 800.0 m”的校验提示，工程终判层面的约束满足仍需补强。

## 下一步优先级

1. 补作者、单位、基金、首页脚注等投稿元信息。
2. 基于真实矿井案例补基线对照、参数标定和工程指标报告。
3. 如需继续压缩工具误报，可将表2改为图片型插表或单独调整 claim audit 规则，但这不影响正文核心论断的人工审核结果。
